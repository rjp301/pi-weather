---
import CenteredCard from "@/layouts/CenteredCard.astro";
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
---

<CenteredCard>
  <form action={`/api/emails`} method="post">
    <Card>
      <CardHeader>
        <CardTitle>Add Email</CardTitle>
        <CardDescription>Add recipients of daily weather email</CardDescription>
      </CardHeader>
      <CardContent>
        <Input type="email" name="email" placeholder="Email" required />
      </CardContent>
      <CardFooter className="grid grid-cols-2 gap-2">
        <Button type="submit">
          <i class="fa-solid fa-save mr-2"></i> Save
        </Button>
        <a href="/emails" class="w-full">
          <Button variant="secondary" type="button" className="w-full">
            <i class="fa-solid fa-cancel mr-2"></i> Cancel
          </Button>
        </a>
      </CardFooter>
    </Card>
  </form>
</CenteredCard>
