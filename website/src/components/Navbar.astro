---
import { Button } from "./ui/button";
import { CardTitle } from "./ui/card";

const pages: { name: string; link: string }[] = [
  { name: "Summary", link: "/" },
  { name: "Emails", link: "/emails" },
  { name: "Stations", link: "/stations" },
];
---

<nav
  class="bg-card shadow border-b flex items-center justify-between px-6 py-2"
>
  <h1 class="font-medium">CGL S34 Weather Summary</h1>
  <ul class="flex gap-2">
    {
      pages.map((page) => {
        const currentPage = Astro.url.pathname === page.link;

        return (
          <li>
            <a href={page.link}>
              <Button variant={currentPage ? "secondary" : "ghost"}>
                {page.name}
              </Button>
            </a>
          </li>
        );
      })
    }
  </ul>
</nav>
